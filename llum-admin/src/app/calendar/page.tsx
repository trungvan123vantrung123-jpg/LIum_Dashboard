export default function CalendarPage() {
  return (
    <div className="max-w-6xl mx-auto p-6">
      <h1 className="text-lg font-medium text-gray-900 mb-4">Lịch phòng</h1>
      <p className="text-sm text-gray-500">
        Trang này sẽ hiển thị lịch dạng lưới, mỗi phòng một hàng, các ngày
        theo cột — đang triển khai ở bước tiếp theo.
      </p>
    </div>
  )
}
